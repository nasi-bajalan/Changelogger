# Changelogger
Merge all the changelogs together.

Install the package and execute
``` changelogger start```
This will create a g folder with the version of your project.

Once you are done adding changes, merge them together using

```changelogger release```
